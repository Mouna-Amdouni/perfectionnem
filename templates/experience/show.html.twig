{% extends 'base.html.twig' %}

{% block title %}Experience{% endblock %}

{% block body %}
<div style="margin-left: 150px">



    <a href="{{ path('experience_index') }}">back to list</a>

    <h5>Experience n°: {{ experience.id }} <div style="margin-left: 90%">
{#            <a  href="{{ path('experience_edit', {'id': experience.id}) }}"><i class="fas fa-edit"></i></a>#}
            {{ include('experience/_delete_form.html.twig') }}
        </div></h5>

    <table class="table">
        <tbody>

            <tr>
                <th>DateFrom</th>
                <td>{{ experience.DateFrom ? experience.DateFrom|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateTo</th>
                <td>{{ experience.DateTo ? experience.DateTo|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ experience.Location }}</td>
            </tr>
            <tr>
                <th>Societe</th>
                <td>{{ experience.societe }}</td>
            </tr>
            <tr>
                <th>LienSociete</th>
                <td>{{ experience.lienSociete }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ experience.position }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ experience.description }}</td>
            </tr>
            <tr>
                <th>LienDescription</th>
                <td>{{ experience.lienDescription }}</td>
            </tr>
        </tbody>
    </table>



</div>
{% endblock %}
