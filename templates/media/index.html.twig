{% extends 'base.html.twig' %}

{% block title %}Media index{% endblock %}

{% block body %}
    <h1>Media index</h1>
    <br/>

    <!-- 16:9 aspect ratio -->
    {% if is_granted('ROLE_DIRECTEUR') and not is_granted('ROLE_ADMIN')  %}
        <a href="{{ path('media_new') }}">Create new</a>
    {% endif %}
{#                {% for medium in media %}#}
                    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/material-design-iconic-font/2.2.0/css/material-design-iconic-font.min.css">
                    <div class="container blog-page">
                        <div class="row clearfix">
                                            {% for medium in media %}

                            <div class="col-lg-4 col-md-12">
                                <div class="card single_post">
                                    <div class="header">
                                        <h3 class="m-t-0 m-b-5"><a href="blog-details.html">{{ medium.titre }}</a></h3>

                                        {#                                        <h2><strong>Video N°</strong> {{  medium.id}}</h2>#}
                                        {% if is_granted('ROLE_DIRECTEUR') and not is_granted('ROLE_ADMIN')  %}

                                            <a href="{{ path('media_delete', {'id': medium.id}) }}">supprimer</a>

                                                               {% endif %}
                                    </div>
                                    <div class="body">
                                        <ul class="meta">
                                            <li><a href="javascript:void(0);"><i class="zmdi zmdi-account col-blue"></i>Posted By: SLIM <strong>JERBIA</strong></a></li>
{#                                            <li><a href="javascript:void(0);"><i class="zmdi zmdi-label col-amber"></i>Technology</a></li>#}
{#                                            <li><a href="javascript:void(0);"><i class="zmdi zmdi-comment-text col-blue"></i>Comments: 3</a></li>#}
                                        </ul>
                                    </div>
                                    <div class="body">
                                        <div class="img-post m-b-15">

                                            <video  width="400" height="222" controls="controls">
                                                <source src=" {{ asset('uploads/images/'  ~ medium.image) }}"alt="Awesome Image"/>
                                           </video>
                                            <div class="social_share">
<h3 style=" color: navajowhite">SLIM <strong>JERBIA</strong>
</h3>                                        </div>
                                        </div>
                                        <p>{{  medium.description}}</p>
                                        <a href="{{ medium.lien }}" title="voir sur youtube " class="btn btn-round btn-info">completer la video sur youtube</a>
                                    </div>




                                </div>

                            </div>



    {% endfor %}          </div>
                        </div>
{% endblock %}
