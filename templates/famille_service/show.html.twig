{% extends 'base.html.twig' %}

{% block title %}FamilleService{% endblock %}

{% block body %}
    <h1>FamilleService</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ famille_service.id }}</td>
            </tr>
            <tr>
                <th>NomFamille</th>
                <td>{{ famille_service.nomFamille }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('famille_service_index') }}">back to list</a>

    <a href="{{ path('famille_service_edit', {'id': famille_service.id}) }}">edit</a>

    {{ include('famille_service/_delete_form.html.twig') }}
{% endblock %}
